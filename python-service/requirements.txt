fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
pdfplumber>=0.11
PyMuPDF>=1.25
markitdown[pdf,docx,xlsx]>=0.1.1
# OpenDataLoader-PDF — local/fast mode structural extractor (Markdown + element
# JSON with bounding boxes). Requires a Java 11+ runtime on the host; the
# pipeline degrades gracefully to the structurer/MarkItDown if it is absent.
opendataloader-pdf>=2.4
