# Frontend production env.
# NOTE: on Vercel this file is NOT used — set VITE_API_URL in the Vercel project's
# Environment Variables (Settings → Environment Variables). Kept here for reference.
#
# TODO after the Render backend is live, set it to that URL + /api/v1, e.g.
#   https://ncism-backend.onrender.com/api/v1
VITE_API_URL=https://TODO-your-render-backend.onrender.com/api/v1
